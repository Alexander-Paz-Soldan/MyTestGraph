package com.example.mytestgraph;

import androidx.appcompat.app.AppCompatActivity;
import android.os.Bundle;
import me.jagar.mindmappingandroidlibrary.Views.Connection;
import me.jagar.mindmappingandroidlibrary.Views.ConnectionTextMessage;
import me.jagar.mindmappingandroidlibrary.Views.Item;
import me.jagar.mindmappingandroidlibrary.Views.ItemLocation;
import me.jagar.mindmappingandroidlibrary.Views.MindMappingView;

public class PractiseWordMap extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_practise_word_map);
    }


    /**
     * For practising with a WordMap we need a WordMap in the first place
     * We need the interaction methods with the user
     * Like hide the words
     * And the statistics such and such right and wrong
     * Store the wrong ones somewhere
     *
     */





}
